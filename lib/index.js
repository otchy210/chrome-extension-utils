const Background = require('./Background');
const Message = require('./Message');
const Tabs = require('./Tabs');
const {LocalStorage, SyncStorage} = require('./Storage');

export {
    Background,
    Message,
    Tabs,
    LocalStorage,
    SyncStorage,
}
