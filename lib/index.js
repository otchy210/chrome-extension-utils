const Background = require('./Background');
const Message = require('./Message');
const Tabs = require('./Tabs');
const {SyncStorage, LocalStorage, SessionStorage} = require('./Storage');

export {
    Background,
    Message,
    Tabs,
    SyncStorage,
    LocalStorage,
    SessionStorage,
}
